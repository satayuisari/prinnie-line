# CLAUDE.md — Prinnie333

คู่มือนี้ให้ Claude (และทุกคนที่แก้โค้ด) อ่านก่อนเริ่มงานเสมอ
เขียน 17 ก.ย. 2569 หลังประกาศรอบแรกทำให้ลูกค้าเข้าใจผิด — bon เป็นคนกำหนดกติกา แก้ได้ที่ไฟล์นี้

## ธุรกิจนี้คืออะไร

Prinnie333 คือบริการดวงส่วนตัวของ **อาจารย์ปรินนี่** (แม่ของ bon) ทางไลน์ @prinnie333
ดวงคำนวณจากวัน เวลา และสถานที่เกิดจริงของแต่ละคน ไม่ใช่ดวง 12 ราศีรวม ๆ

| อะไร | ราคา | ได้อะไร |
|---|---|---|
| ฟรี | 0 | พื้นดวง (อาทิตย์ จันทร์ ลัคนา) · ไพ่ทาโรต์ประจำสัปดาห์ · ผูกดวงคู่ดูคะแนน % |
| สมาชิก | 399 บาท / 30 วัน | ดวงรายวันเฉพาะตัวทุกเช้า 08:00 · สิทธิ์ลุ้นดูดวงกับอาจารย์ |
| ผูกดวงคู่แบบเต็ม | 199 บาท ครั้งเดียว | ผลเต็ม |

จ่ายเงินทางพร้อมเพย์ + ส่งสลิป (SlipOK ตรวจอัตโนมัติ) Stripe/Beam ไม่รับธุรกิจดูดวง

## จับรางวัลดูดวงกับอาจารย์ (กติกาจาก bon)

> "399 ใครเป็นสมาชิกมีสิทธิ์ลุ้นดูดวงกับอาจารย์ โดยเราจะประกาศชื่อเมื่อถึงเวลา ให้นัดดูหลังจากนั้น"

- **ใครมีสิทธิ์ลุ้น:** สมาชิกที่จ่ายจริง ยังไม่หมดอายุ เป็นสมาชิกต่อเนื่องครบ 14 วัน คนที่เคยได้แล้วเว้น 12 เดือน
- **เมื่อไร:** วันที่ 2 และ 17 ของเดือน 09:00 รอบละ 1 ท่าน (`src/scheduler/loyaltyRewards.js`)
- **จับยังไง:** จับจริง ตรวจสอบย้อนได้ — seed `prinnie-<รอบ>` + sha256 บนรายชื่อที่เรียงแล้ว (`monthlyPick.drawOne`) seed บันทึกไว้ใน `loyalty_rewards.note`
- **หลังจับ:** ส่งข้อความถึงผู้ได้รับ → ประกาศชื่อในไลน์ทั้งสองบัญชี → แจ้งแอดมิน → ทีมงานนัดเวลากับอาจารย์
- ประกาศผลตามเวลาที่กำหนด: ตั้ง `PICK_ANNOUNCE_AT` (ยิงครั้งเดียว ประกาศผู้ได้รับล่าสุด) — รอบ 17 ก.ย. ผลออก 09:00 ประกาศทางการ 20:00
- ปิดทั้งระบบ `LOYALTY_ENABLED=false` · ปิดเฉพาะประกาศ `LOYALTY_ANNOUNCE=false`

## กติกาการเขียนข้อความถึงลูกค้า (สำคัญที่สุด)

bon: *"ให้คุยภาษาไทยรู้เรื่อง ไม่ใช่ประกาศให้คนอื่นเข้าใจผิด ต้องชัดเจนชัดถ้อยชัดคำ"*

1. **ภาษาไทยธรรมดา** ประโยคสั้น อ่านรอบเดียวเข้าใจ ไม่ใช้คำโฆษณาหรือคำแปลกหู
2. **ข้อความที่ส่งถึงทุกคน ห้ามเขียนให้ใครเข้าใจว่าตัวเองได้อะไรแล้ว** บอกให้ชัดว่าใครได้ ได้กี่คน และต้องเป็นสมาชิก
3. **ไม่ใช้ศัพท์โหราศาสตร์ในประกาศ** (ทำมุม ตรีโกณ จตุรัส) ดวงรายวันใช้ได้ตามปกติ เพราะเป็นคำของอาจารย์
4. **คำว่า "จับรางวัล" "ลุ้น" "ผู้ได้รับรางวัล" ใช้ได้** — bon ยกเลิกข้อห้ามเดิมแล้ว 17 ก.ย.
5. **ห้ามใช้ "ดวงเลือกคุณ" เป็นหัวข้อความ** — หัวนี้ทำให้ทุกคนคิดว่าตัวเองได้
6. **ข้อความใหม่ที่จะส่งถึงลูกค้าทุกอัน ต้องวางให้ bon อ่านในแชทก่อนตั้งเวลาส่ง** (วางเป็นข้อความ ไม่ใช่ไฟล์)
7. น้ำเสียงอิงจากดวงรายวันของอาจารย์: พูดกับผู้อ่านว่า "คุณ" ตรง ๆ อบอุ่น เล่าเรื่อง ไม่ขายของ
8. ใช้สกิล `thai-writing` (~/.claude/skills) ตรวจก่อนส่ง
9. ด่าน `assertClean` (`src/services/pickAnnounce.js`) กันถ้อยคำที่เคยทำให้เข้าใจผิด — เพิ่มคำได้เมื่อเจอเคสใหม่

## บอทตอบแชท (AI)

- `src/services/supportAI.js` — คำสั่งของบอทเขียนไว้ใน `SYSTEM`
- **ทุกครั้งที่มีโปร/โปรแกรม/ราคาใหม่ ต้องอัปเดต `SYSTEM` ด้วย** ไม่งั้นบอทจะบอกลูกค้าว่าไม่มี (เกิดจริง 17 ก.ย. บอทบอกผู้ได้รางวัลว่าเป็นข้อความหลอกลวง)
- ผู้ได้รางวัลที่ยังไม่ได้นัด บอทไม่ตอบ ส่งแจ้งแอดมินแทน (`src/routes/webhook.js`)
- เปิด/ปิดด้วย `AI_AUTOREPLY` บน Railway — ปิด 17 ก.ย. เช้า · **bon สั่งเปิดคืน 17 ก.ย. 10:10** หลังทดสอบคำถามยากผ่าน · ถ้าบอทตอบมั่วอีก ปิดก่อนแล้วค่อยบอก bon

## ระบบอัตโนมัติ (ทั้งหมดใน `src/scheduler/`)

| งาน | เวลา | ไปถึงใคร |
|---|---|---|
| ดวงรายวัน | 08:00 | สมาชิกทุกคน |
| รายงานผล 12 ชม. | 08:00 · 20:00 | แอดมิน |
| เตือนต่ออายุ | 09:00 | สมาชิกที่จะหมดพรุ่งนี้ |
| จับรางวัล | 2 และ 17 · 09:00 | ผู้ได้รับ + ประกาศทุกคน |
| วัดผลคอนเทนต์ | 09:30 | (เงียบ) |
| เตือนออเดอร์ค้าง | 10:30 | ลูกค้าที่ยังไม่โอน |
| โต๊ะคอนเทนต์ เสนอโพสต์พรุ่งนี้ | 17:00 | แอดมิน (bon + แม่) |
| สรุปมุมคอนเทนต์ | จันทร์ 09:40 | แอดมิน |
| ตรวจสลิปซ้ำ | ทุก 5 นาที | — |

แอดมิน = `ADMIN_USER_IDS` (bon + แม่)

## Deploy

- **Railway ไม่ auto-deploy จาก GitHub แล้ว** (พบ 16 ก.ย.) ต้องสั่งเอง:
  `npx @railway/cli up --service prinnie-app --environment production --detach`
- เช็คว่าขึ้นจริงจาก log บรรทัดเริ่มต้นของแต่ละงาน
- ตัวแปรดู/ตั้งด้วย `npx @railway/cli variables --service prinnie-app --environment production`
- ฐานข้อมูล: Railway Postgres (`Postgres`) · ถ้าดับ ต้องให้ bon สั่ง redeploy เอง
- อ่านข้อมูลโปรดักชันแบบอ่านอย่างเดียว: `npx @railway/cli ssh --service prinnie-app` แล้วรัน node ใน `/app`

## ก่อนบอกว่าเสร็จ

```bash
npm test        # PGlite แยกโฟลเดอร์ ไม่แตะข้อมูลจริง · ต้องเขียวทั้งหมด
```

## ข้อมูลส่วนตัว

- ห้ามเอาข้อมูลลูกค้าหรือของครอบครัวไปไว้ในที่สาธารณะ (Artifact เว็บ ฯลฯ)
- ห้ามพิมพ์ LINE user id เต็มลง log หรือแชท ใช้ตัดหัว 9 ตัวอักษร
- คีย์อยู่บน Railway เท่านั้น ไม่เข้า git
